// Sticky 分发层 —— 微信/H5 端实现
// 透传 NutUI（babel-plugin-import 按需引入组件与样式），类型与 NutUI 一致。
// RN 端编译时由同目录 index.rn.tsx 替代（rn-supporter 平台后缀优先解析）。
import { Sticky } from '@nutui/nutui-react-taro'

export default Sticky
